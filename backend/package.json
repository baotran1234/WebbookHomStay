{
  "name": "my-project-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "api": "json-server --watch db.json --port 3010",
    "payment:dev": "cross-env PAYMENT_PORT=4010 node server/index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "json-server": "^1.0.0-beta.3"
  }
}
